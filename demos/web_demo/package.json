{
  "name": "stinger-web-demo-server",
  "version": "1.0.0",
  "description": "Production server for Stinger web demo",
  "main": "serve-production.js",
  "scripts": {
    "install:all": "npm install && cd frontend && npm install && cd ../backend && pip install -r requirements.txt",
    "build:frontend": "cd frontend && npm run build",
    "start:backend": "cd backend && python main.py",
    "start:frontend:dev": "cd frontend && npm start",
    "start:frontend:prod": "node serve-production.js",
    "start:all": "npm run build:frontend && npm run start:frontend:prod",
    "serve": "npm run start:all"
  },
  "dependencies": {
    "express": "^4.18.2",
    "http-proxy-middleware": "^2.0.6"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=14.0.0"
  }
}